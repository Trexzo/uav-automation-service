# Security policy

Do not open a public issue containing bot tokens, webhook URLs, API tokens,
private deployment profiles, live channel/server identifiers, or runtime data.
Revoke exposed credentials immediately before reporting the issue privately to
the maintainer.

The repository's security scan is intended as a guardrail, not a substitute for
reviewing staged files before every push.
