# Contributing

Use synthetic examples in tests, documentation, screenshots, and configuration
samples. Never submit a real deployment profile or runtime state.

Before opening a pull request:

```bash
pytest
python scripts/security_scan.py
```
